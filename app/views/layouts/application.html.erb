<!DOCTYPE html>
<html>
  <head>
    <title><%= t('site_title') %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-dark bg-dark">
      <div class="container-fluid">
        <ul class="nav bg-dark text-white">
          <li class="nav-item">
            <a class="nav-link active text-white" aria-current="page" href="/"><%= "#{t 'site_title'}" %></a>
          </li>

				<% if user_signed_in? %>
          <li class="nav-item ">
            <%= link_to "#{t 'my_profile_page_title' }", edit_user_registration_path, class: "nav-link text-white" %>
          </li>
          <li class="nav-item ">
            <%= link_to "#{t 'actions.sign_out' }", destroy_user_session_path, :method => :delete, class: "nav-link text-white" %>
          </li>
				<% end %>
        </ul>
    </div>
    </nav>
    <% if notice %>
      <p class="notice alert alert-primary"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
